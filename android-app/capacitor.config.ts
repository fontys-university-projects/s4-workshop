import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.martin.androidApp',
  appName: 'android-app',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
